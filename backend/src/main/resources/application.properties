spring.application.name=coordifit

spring.profiles.active=local

# MyBatis Configuration
mybatis.mapper-locations=classpath:mapper/**/*.xml
mybatis.type-aliases-package=com.miracle.coordifit.*.model
mybatis.configuration.map-underscore-to-camel-case=true
mybatis.configuration.jdbc-type-for-null=NULL

app.cors.allowed-origins=http://localhost:5173,https://www.coordifit.com,https://api.coordifit.com

# Python FastAPI Container Connection Setting
app.ai.connect-timeout-ms=3000

# Server Configuration
server.port=8080

# Email Configuration (for email verification)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=coordifit.noreply@gmail.com
spring.mail.password=diwm awwa jcbw xadk
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Swagger API Settings
springdoc.api-docs.enabled=true
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
springdoc.swagger-ui.use-root-path=true

# Gemini API
app.google.gemini.base-url=https://generativelanguage.googleapis.com
app.google.gemini.timeout-ms=10000

# File upload max file size
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Redis Configuration (for JWT token storage)
spring.data.redis.port=6379
spring.data.redis.timeout=2000ms
spring.data.redis.jedis.pool.max-active=8
spring.data.redis.jedis.pool.max-idle=8
spring.data.redis.jedis.pool.min-idle=0

# JWT Configuration
jwt.secret=coordifit-jwt-secret-key-2024-very-long-and-secure-key-for-production
jwt.access-token-expiration=3600000
jwt.refresh-token-expiration=604800000

# For Thumbnail Image Naming Suffix
file.thumbnail.suffix=_thumb

app.openai.base-url=https://api.openai.com
app.openai.timeout-ms=10000

# log4jdbc log level setting
logging.level.root=INFO
logging.level.com.miracle.coordifit=DEBUG

logging.level.log4jdbc.log4j2.sqltiming=DEBUG
logging.level.log4jdbc.log4j2.sqlonly=OFF
logging.level.log4jdbc.log4j2.audit=OFF
logging.level.log4jdbc.log4j2.resultset=OFF
logging.level.log4jdbc.log4j2.resultsettable=OFF
logging.level.log4jdbc.log4j2.connection=OFF

